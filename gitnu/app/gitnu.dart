library terminal;import 'dart:html';//import 'dart:js';import 'lib/terminal-dart/terminal_filesystem.dart';void main() {  new TerminalFilesystem().run();}// List of acceptable commands to be passed to the terminal/*var commandList = {                    'ls': lsHandler,                    'cd': cdHandler,                    'git': gitHandler,                    'help': helpHandler                    };void helpHandler(term) {  var helpText = "Gitnu terminal, version 0.1\nThese commands are defined internally. Type 'help' to see this list.";  helpText += "\n\nls [FILE]\ncd [FILE]\ngit <command>";  term.echo(helpText);}void lsHandler(term, args) {    term.echo("ls");}void cdHandler(term, args) {    term.echo("cd");}void gitHandler(term, args) {    term.echo("git");    var option = args.shift();    switch(option) {        case "commit":            term.echo("commit");            term.echo(args.toString());            break;        case "clone":            term.echo("clone");            term.echo(args.toString());            break;        default:             term.echo("unknow option - " + option);    }}void commandHandler(command, term) {  var commandInput = command.split(" ");  var functionCall = commandInput.shift();  if(commandList.containsKey(functionCall)) {    commandList[functionCall](term, commandInput);    } else {    term.echo(functionCall + ': command not found');  }}*/